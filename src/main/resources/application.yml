#端口号
server:
  port: 8083


spring:
  mvc:
    view:
      prefix : /WEB-INF/
      suffix : .jsp



#mybatis配置
mybatis:
  configuration:
    map-underscore-to-camel-case: true  #按驼峰命名规则自动匹配到实体类上,不需要用别名
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.boke_houtai.pojo
